# A release is autogenerated with the install wizard.
# https://github.com/acceleratedscience/openad-model-helm-template

# releases:
  # - name: openad-model
  #   namespace: openad-models
  #   chart: ./helm
releases:
  - name: smi-ted-openad-model
    namespace: openad-models
    chart: ./smi-ted-openad-model
